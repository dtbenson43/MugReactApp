{
  "routes": [
      {
          "route": "/*",
          "rewrite": "/"
      }
  ],
  "navigationFallback": {
      "rewrite": "/",
      "exclude": [
          "*.{jpg,gif,png}",
          "assets/*"
      ]
  }
}